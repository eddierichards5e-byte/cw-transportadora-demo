# Demonstração web

A demonstração usa um banco isolado e não deve receber credenciais ou dados de produção.
Para produção, configure `CW_DATABASE_URL` para PostgreSQL/Supabase e um `CW_WEB_SECRET` com pelo menos 32 caracteres.
